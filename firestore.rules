rules_version = '2';

service cloud.firestore {
  match /databases/{database}/documents {
    // ブラウザからFirestoreへ直接アクセスさせない。
    // 依頼の登録は Cloud Functions の Admin SDK 経由のみ。
    match /{document=**} {
      allow read, write: if false;
    }
  }
}
